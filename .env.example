# ===========================================
# TW Analytics 环境配置
# ===========================================

# 🔑 Twitter API配置
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# 🚀 多数据源支持 - Playwright爬虫配置 (解决API限流问题)
# 账户信息现在通过统一账户管理系统配置 (src/config/accounts.json)
# TWITTER_USERNAME=  # 请使用账户管理系统配置多个账户
# TWITTER_PASSWORD=  # 请使用账户管理系统配置账户密码
# TWITTER_EMAIL=     # 请使用账户管理系统配置备用邮箱

# 🎭 Playwright浏览器配置
PLAYWRIGHT_HEADLESS=false  # 开发时设为false可观察登录过程
PLAYWRIGHT_PROXY=http://127.0.0.1:7890  # 代理配置，支持 http://, https://, socks5://
# PLAYWRIGHT_PROXY=socks5://127.0.0.1:7890  # SOCKS5代理示例
# COOKIE_FILE_PATH=instance/twitter_cookies.json  # 已移除cookie系统

# 🌐 智能代理管理器配置 (Fast Booster)
NETWORK_MODE=auto                # 网络模式: auto(自动), direct(直连), local_proxy(本地代理), proxy_pool(代理池)
LOCAL_PROXY=127.0.0.1:7890      # 本地代理地址(科学上网用)
PROXY_POOL_ENABLED=true         # 是否启用代理池(业务需求)
PROXY_POOL_FILE=scripts/proxies.txt  # 代理池文件路径

# 📝 注意：代理优先级说明
# 1. API参数 proxy (最高优先级) - 直接在请求中指定单一代理
# 2. API参数 use_proxy_pool (高优先级) - 请求级别控制是否使用代理池
# 3. 环境变量 (默认值) - 全局默认设置，可被API参数覆盖

# 🚀 Apify 集成配置 (第三方爬取服务)
APIFY_ENABLE=true              # 是否启用Apify数据源
<<<<<<< HEAD
APIFY_API_TOKEN=__APIFY_TOKEN__  # Apify API令牌 (从 https://console.apify.com/account/integrations 获取)
=======
APIFY_API_TOKEN=  # Apify API令牌 (从 https://console.apify.com/account/integrations 获取)
>>>>>>> 6574c9d (update)
APIFY_ACTOR_ID=apidojo/tweet-scraper       # 使用的Actor ID (默认推特爬取器)
APIFY_TIMEOUT=120               # Actor运行超时时间(秒)

# 🔄 数据源优先级配置
DATA_SOURCE_PRIORITY=playwright,apify,twitter_api  # 数据源优先级 (逗号分隔，支持: playwright, apify, twitter_api)

# 📝 Flask应用配置 (可选，有默认值)
# SECRET_KEY=your-secret-key-here
# FLASK_DEBUG=true
# HOST=127.0.0.1
# PORT=5100

# 📊 API限制配置 (可选，有默认值)
# MAX_TWEETS_PER_REQUEST=100
# MAX_BATCH_SIZE=50
# DEFAULT_TWEET_COUNT=10

# 📋 日志配置 (可选，有默认值)
# LOG_LEVEL=INFO

# ===========================================
# 使用说明：
# 1. 复制此文件为 .env 
# 2. 填入你的 TWITTER_BEARER_TOKEN
# 3. 如需启用多数据源，填入 Twitter 账号信息
# 4. 本地开发建议设置代理配置
# 5. 智能代理管理器配置说明：
#    环境变量设置：
#    - NETWORK_MODE=auto: 自动检测网络环境，智能选择代理
#    - NETWORK_MODE=direct: 强制直连，不使用任何代理
#    - NETWORK_MODE=local_proxy: 强制使用本地代理(科学上网)
#    - NETWORK_MODE=proxy_pool: 强制使用代理池(业务需求)
#    - LOCAL_PROXY: 本地代理地址，用于科学上网访问x.com
#    - PROXY_POOL_ENABLED: 是否启用代理池功能
#    
#    API参数覆盖（优先级更高）：
#    - "proxy": "http://proxy.com:8080" (最高优先级，覆盖所有环境变量)
#    - "use_proxy_pool": false (覆盖 PROXY_POOL_ENABLED 环境变量)
# 6. Apify 集成配置说明：
#    - 数据源优先级：Playwright > Apify > TwitterAPI
#    - APIFY_ENABLE=true: 启用Apify作为备用数据源
#    - APIFY_API_TOKEN: 从 Apify Console 获取的API令牌
#    - APIFY_ACTOR_ID: 推特爬取器的Actor ID（推荐使用默认值）
#    - APIFY_TIMEOUT: Actor运行超时时间，建议2-5分钟
# ==========================================
